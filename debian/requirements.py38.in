# This file must be copied to the root of the Pontoon's repository,
# Then the requirements.py38.txt can be generated with the following command:
#
#     pip-compile --generate-hashes requirements.py38.in
#

-r requirements.txt

# Python 3.8 additional dependency for pytz-deprecation-shim
backports.zoneinfo==0.2.1
